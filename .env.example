# Public browser configuration. Replace *** only in an untracked local .env file.
NEXT_PUBLIC_SUPABASE_URL=***

# Server-only values. Never import these from browser/client modules.
SUPABASE_SERVICE_ROLE_KEY=***
RATE_LIMIT_HMAC_KEY=***
CRON_SECRET=***
