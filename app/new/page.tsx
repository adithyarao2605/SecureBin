import { AppWorkspace } from "../components/app-workspace";

export const metadata = {
  title: "New share — SecureBin",
  description: "Create a browser-encrypted SecureBin share.",
};

export default function NewSharePage() {
  return <AppWorkspace />;
}
